Razor es c# + html
Views renderizan la informacion que da el Controller
El controller maneja las soliciutdes del Router y las pasa a la vista o al Model que accede a la DB para buscar la informacion que le devuelve a la vista para  poder mostrarla

prop 2x tab -> setea los atributos de la clase que estamos generadndo
ctor 2xtab -> shortcut apra el constructor de la clase


ORM -> Object Relationship Manager
